name: density-eqgnn
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pytorch=2.5.1
  - cpuonly
  - numpy
  - pymatgen
  - mp-api
  - pyyaml
  - tqdm
  - pytest
variables:
  PYTHONPATH: src
